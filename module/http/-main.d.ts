
import Method from './Method'

export default interface http {
  /**
   * In Oracle's documentation,
   * the enum value "PATCH" is listed under values
   * although it does not actually exist on the enum
   */
  Method: Method
}
